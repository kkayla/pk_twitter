# the rake file locates and loads tasks that can be run from the command line
require "./app"
require "sinatra/activerecord/rake"
# now go into terminal and create your rake database, you'd type in 'rake db:create'
